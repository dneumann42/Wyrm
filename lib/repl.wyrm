fun start-repl {} do
    var running 1
    while {> $running 0} do
        var inp [read-line "> "]
        if {[ = $inp "q" ]} do
            running= 0
        else
            var res [try {eval [read $inp]}]
            print $res
        end
    end
end

export start-repl
